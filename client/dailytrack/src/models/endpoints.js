const YOUR_API_PORT = ":3000"

const api_url = 'http://10.0.2.2:3000/v1';

const user_url = `${api_url}/users`;
const signin_url = `${api_url}/users/signin`;

export { api_url, user_url, signin_url };